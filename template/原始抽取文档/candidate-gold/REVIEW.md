# 原始抽取文档候选标准答案审阅

这些 JSON 是由一次 `full_document` 抽取生成的候选答案，不是已经确认的 gold。下面的结论来自候选值与原始文档的逐项核对；标记为“需确认”的字段不能直接用于三种策略评分。

## CTP / EMR062202-060

文件：`1.2-CTP_EMR06220-060_SCCHN_CHANGE2_Sign_Combine-cn_副本.pdf`

已确认或有直接依据：方案号、III 期、开放性、国内多中心、适应症、样本量 240、研究设计、主要/次要终点、入选/排除标准、研究药物和给药规格、注册号 `CDEL20140112`。

需要修改或确认：

- `planned_start_date` / `planned_end_date`：原文是“2015 年第 2 季度–2017 年第 4 季度”，候选答案把季度擅自转成了 4 月 1 日和 12 月 31 日；当前 Schema 的日期规则不足以表达季度，应标记为待定或扩展精度字段。
- `project_owner`：原文明确的是协调研究者林桐榆，不一定等同于项目负责人。
- `project_owner_phone`、`project_owner_email`：当前 PDF 文本未找到直接证据，应删除或标记 unsupported。
- `registration_type=注册类`、`drug_registration_class=治疗用生物制品`：候选值来自业务推断，不是文档明确字段，应人工确认。
- 对照产品剂型“注射液”需要确认；原文明确静脉输注，但未必直接给出剂型。
- 顺铂发生毒性时允许卡铂替代，`control_products` 是否需要包含卡铂需按业务 Schema 决定。
- `protocol_version` 建议保留“2014 年 11 月 17 日/终版 1.0”的版本日期信息，不能只保留 `V1.0` 后丢失日期。

## FOT12-CN-301

文件：`Full protocol_FOT12-CN-301-Ver1 0-18Mar2015-20150407F_副本.pdf`

已确认或有直接依据：方案号、方案名称、普拉曲沙、适应症、III 期、国内多中心、68 例、开放单组设计、主要终点 ORR、次要终点、Pralatrexate、注射液、20 mg/mL，以及入选/排除标准主体。

需要修改或确认：

- `registration_type=注册类`：原文表达“支持中国注册批准”，但没有明确使用“注册类”作为字段值，应标记为推断待确认。
- `protocol_version` 建议保留版本日期 `2015-03-18`，不能只保留 `V1.0`。
- 长列表需要逐条对照正文，不能因为候选答案来自 full-document 就自动视为 gold。

## 西达本胺 ACE / CDM301

两个 DOCX 文件哈希相同，合并为一个样本；测试输入暂转换成 PDF。

文件：`西达本胺乳腺癌III期试验方案_v1.0 20150421-0708 Final_副本.docx`

已确认或有直接依据：方案号 CDM301、版本 1.0、药物西达本胺、联合依西美坦、5 mg/片、25 mg/片、英文药名、批件号 `2010L02920`、双盲期样本量 328、主要终点 PFS、联合产品。

需要修改或确认：

- `project_name` / `protocol_name` 当前只保留英文标题；原文首页有中文标题“西达本胺联合依西美坦治疗激素受体阳性晚期乳腺癌的 III 期临床试验”，应确认 Schema 期望中文还是英文。
- `indication` 当前只有“激素受体阳性晚期乳腺癌”，漏掉“经内分泌治疗失败”的限定。
- `planned_subject_count` / `sample_size=328例` 只对应双盲对照期；原文另有开放期 20 例。必须明确字段表示总样本还是双盲期样本。
- `primary_endpoint=PFS` 只适用于双盲对照期，不能代表开放期全部研究终点。
- `nmpa_document_no` 的号码准确，但原文称 CFDA 临床批件号；字段标签需要确认是否沿用 NMPA 统一命名。

## 施贵宝 CA209-078

文件：`施贵宝- CA209-078 - 修订方案01-摘要-中文_副本.pdf`

该 PDF 是扫描型文档，普通 `pdftotext` 无有效正文。候选答案来自 MinerU 解析结果，当前只能作为待复核候选；试验组/对照组、500 例、终点和排除标准应通过页面截图或 OCR 结果逐项确认后，才能作为正式 gold。

## 结论

当前 4 份文件都还不能直接作为正式标准答案。至少需要先确认：CTP 日期精度、CTP 联系方式和注册分类、FOT12 注册类型、Xida 的样本量范围/中文适应症，以及 CA209-078 的扫描 OCR 结果。确认这些字段后再运行 `full_document`、`source_page_routed` 和 `agentic_source_page_routed`，否则比较的是模型输出与模型输出生成的候选答案，结论会失真。
